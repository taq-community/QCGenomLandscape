% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncbi_sequences.R
\name{fetch_ncbi_sequences}
\alias{fetch_ncbi_sequences}
\title{Fetch NCBI nucleotide records for a set of species/marker queries}
\usage{
fetch_ncbi_sequences(
  queries,
  retmax = 5000,
  batch_size = 200,
  high_id_threshold = 500,
  search_fn = rentrez::entrez_search,
  summary_fn = rentrez::entrez_summary,
  progress = TRUE
)
}
\arguments{
\item{queries}{Character vector of Entrez query strings, e.g. from
\code{\link[=build_ncbi_queries]{build_ncbi_queries()}}}

\item{retmax}{Integer, per-search max IDs to retrieve, default 5000}

\item{batch_size}{Integer, \code{entrez_summary} batch size, default 200}

\item{high_id_threshold}{Integer, queries returning more IDs than this are
flagged in \code{high_id_queries}, default 500}

\item{search_fn}{Function with signature \verb{(db, term, retmax)}, default
\code{\link[rentrez:entrez_search]{rentrez::entrez_search()}}}

\item{summary_fn}{Function with signature \verb{(db, id)}, default
\code{\link[rentrez:entrez_summary]{rentrez::entrez_summary()}}}

\item{progress}{Logical, show a progress bar, default \code{TRUE}}
}
\value{
A list with three elements: \code{results} (tibble of parsed sequence
summaries), \code{deficient_queries} (list of queries that errored), and
\code{high_id_queries} (list of queries whose ID count exceeded \code{high_id_threshold})
}
\description{
The batching/error-handling loop shared by the voucher and non-voucher
NCBI query scripts. \code{search_fn}/\code{summary_fn} are injectable so the
batching, high-ID-count flagging, and error-accumulation logic can be unit
tested without live network access or an \code{NCBI_API_KEY}.
}
