% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genbank.R
\name{is_complete_genome}
\alias{is_complete_genome}
\title{Flag whether a GenBank record's DEFINITION describes a complete genome}
\usage{
is_complete_genome(definition)
}
\arguments{
\item{definition}{Character vector, e.g. \code{\link[=parse_gb_records]{parse_gb_records()}}'s
\code{definition} column}
}
\value{
Logical vector, same length as \code{definition}; \code{NA} where
\code{definition} is \code{NA}
}
\description{
GenBank distinguishes \code{"complete genome"} from \code{"partial genome"} in its
own DEFINITION wording (e.g. \code{"Boreogadus saida ... mitochondrion, complete genome."} vs \code{"... mitochondrion, partial genome."}) -- the
authoritative signal for "is this a full genome", direct from the
submitter, rather than inferring it from how many \verb{/gene=} tags happen to
be annotated on the record (a multi-gene \emph{fragment} spanning just two
adjacent genes would also have multiple tags without being anywhere near
a full genome).
}
\examples{
is_complete_genome(c(
  "Boreogadus saida mitochondrion, complete genome.",
  "Boreogadus saida mitochondrion, partial genome.",
  "Boreogadus saida cytochrome oxidase subunit I (COI) gene, partial cds."
))
}
