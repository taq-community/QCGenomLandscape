% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequence_qc.R
\name{flag_length_outliers}
\alias{flag_length_outliers}
\title{Flag sequence-length outliers within each marker/gene group}
\usage{
flag_length_outliers(seq_length, group, k = 5, min_n = 10)
}
\arguments{
\item{seq_length}{Numeric vector of sequence lengths}

\item{group}{Vector (e.g. \code{gene_group} from \code{\link[=assign_gene_group]{assign_gene_group()}})
identifying which group each length belongs to}

\item{k}{Numeric, threshold in MAD units, default 5 (a conservative bar --
under normality this flags roughly the most extreme ~0.0001\% of a
distribution, well past ordinary biological length variation)}

\item{min_n}{Integer, minimum group size to attempt outlier detection,
default 10}
}
\value{
Logical vector, same length as \code{seq_length} -- \code{TRUE} for
outliers, \code{NA} where \code{seq_length} is \code{NA} or the group is smaller than
\code{min_n}
}
\description{
Robust (median + MAD) outlier detection, run independently per \code{group} --
a fixed length threshold isn't meaningful across markers as different as
a ~650bp COI barcode fragment and a ~1500bp rbcL sequence, and hardcoding
a per-marker "expected length" table would need constant upkeep as new
marker types show up in the data. MAD-based (not SD-based) specifically
because SD is itself inflated by the outliers being detected; groups
smaller than \code{min_n} are left unflagged (\code{NA}) since there isn't enough
data in a tiny group to say what's "normal".
}
\examples{
flag_length_outliers(c(640, 655, 648, 651, 5), rep("COI", 5), min_n = 3)
}
