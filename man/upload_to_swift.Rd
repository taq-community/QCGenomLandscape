% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swift.R
\name{upload_to_swift}
\alias{upload_to_swift}
\title{Upload a local file to Arbutus (OpenStack Swift) object storage}
\usage{
upload_to_swift(
  path,
  object_name = basename(path),
  storage_url =
    "https://object-arbutus.alliancecan.ca/swift/v1/cc7d995026f443268e822676773a4252",
  container = "QCGenomicLandscape",
  token = swift_auth(),
  request_fn = httr2::request
)
}
\arguments{
\item{path}{Character, path to the local file to upload}

\item{object_name}{Character, name of the object in the container,
default \code{basename(path)}}

\item{storage_url}{Character, the Swift account storage URL (not secret --
this is the public endpoint shared for this project), default the
QCGenomicLandscape Arbutus account}

\item{container}{Character, container (bucket) name, default \code{"QCGenomicLandscape"}}

\item{token}{Character, a Keystone auth token, default \code{\link[=swift_auth]{swift_auth()}}.
Pass an explicit token when uploading multiple files to avoid
re-authenticating for each one.}

\item{request_fn}{Function with signature \code{(url)} returning an \code{httr2}
request, default \code{\link[httr2:request]{httr2::request()}}}
}
\value{
Invisibly, the uploaded object's path within the container
(\code{"{container}/{object_name}"})
}
\description{
Upload a local file to Arbutus (OpenStack Swift) object storage
}
