% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequence_qc.R
\name{build_sequence_qc_table}
\alias{build_sequence_qc_table}
\title{Build the per-record sequence-QC table from fetched GenBank batches}
\usage{
build_sequence_qc_table(
  gb_records,
  log_every_batch = 500,
  log_every_seq = 20000,
  progress = TRUE,
  cores = NULL
)
}
\arguments{
\item{gb_records}{List of raw GenBank flat-file blobs, e.g. from
\code{\link[=fetch_gb_records]{fetch_gb_records()}}}

\item{log_every_batch}{Integer, chunk size for both progress logging and
parallel dispatch of the batch-parsing step, default 500}

\item{log_every_seq}{Integer, log a progress line every this many
stop-codon checks, default 20000}

\item{progress}{Logical, also show \code{purrr} progress bars, default \code{TRUE}}

\item{cores}{Integer, cores to use for parallel batch parsing, default
\code{NULL} meaning \code{parallel::detectCores() - 1} (at least 1). Deliberately
conservative rather than using every core, since each worker holds its
own copy of the batch text and this step has previously run under heavy
memory pressure.}
}
\value{
Tibble with columns \code{accession}, \code{definition}, \code{gene}, \code{sequence},
\code{seq_length}, \code{n_count}, \code{n_pct}, \code{gc_pct}, \code{has_stop},
\code{is_complete_genome} (see \code{\link[=is_complete_genome]{is_complete_genome()}} -- GenBank's own
"complete genome" wording, not inferred from the gene count)
}
\description{
Parses raw GenBank flat-file batches, scores basic sequence-quality
metrics, and flags in-frame stop codons (COI/SGC4 code). Logs progress
periodically via \code{logger} -- at BDQC scale (thousands of batches,
hundreds of thousands of sequences) this step has no other progress
signal and can run for hours, particularly the per-sequence stop-codon
check (one \code{Biostrings} translation call per sequence).
}
\details{
Batch parsing is parallelized with \code{\link[parallel:mclapply]{parallel::mclapply()}} (fork-based --
Linux/macOS only; falls back to a single core on Windows, since forking
isn't available there) since each batch parses independently of the
others.
}
