% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequence_qc.R
\name{flag_barcode_gap_outliers}
\alias{flag_barcode_gap_outliers}
\title{Flag DNA sequences whose distance to conspecifics is a group outlier}
\usage{
flag_barcode_gap_outliers(
  sequence,
  species,
  group,
  k = 5,
  min_n = 3,
  max_n = 200,
  cores = NULL,
  log_every = 200
)
}
\arguments{
\item{sequence}{Character vector of DNA sequences}

\item{species, group}{Vectors identifying species and marker/gene group;
sequences are only compared within the same (species, group)}

\item{k}{Numeric, threshold in MAD units, default 5}

\item{min_n}{Integer, minimum (species, group) size to attempt, default 3}

\item{max_n}{Integer, (species, group) combinations larger than this are
skipped rather than aligned, default 200}

\item{cores}{Integer, cores to use for parallel alignment, default
\code{NULL} meaning \code{parallel::detectCores() - 1} (at least 1)}

\item{log_every}{Integer, log a progress line every this many aligned
(species, group) combinations, default 200}
}
\value{
Logical vector, same length as \code{sequence} -- \code{NA} where not
attempted (group too small/large, alignment failed, or \code{NA} input)
}
\description{
The "barcode gap" idea from DNA barcoding QC: for each (species, group)
with at least \code{min_n} sequences, aligns them with \code{\link[=align_sequences]{align_sequences()}}
(DECIPHER) and computes a pairwise distance matrix, then flags sequences
whose mean distance to the rest of the group is a robust (median + MAD)
outlier -- a sequence far outside its own species' normal intraspecific
variation is a candidate misidentification or contamination, rather than
a fixed cross-taxon distance threshold, which wouldn't hold uniformly
across markers/taxa.
}
\details{
This is the most expensive of the sequence-QC checks -- multiple sequence
alignment scales worse than linearly with group size. Each (species,
group) aligns independently of every other, so the groups are
parallelized with \code{\link[parallel:mclapply]{parallel::mclapply()}} (fork-based -- Linux/macOS
only; falls back to a single core on Windows), the same pattern used for
batch parsing in \code{\link[=build_sequence_qc_table]{build_sequence_qc_table()}}; \code{max_n} still skips groups
too large to align cheaply rather than trying and stalling one worker.
Each worker's alignment is wrapped in \code{try()} -- not just \code{mclapply()}'s
own error containment, which only isolates a failure when it actually
forks and silently stops containing errors at all if forking isn't
available -- so one bad group can't take down the whole call.
}
