% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{parse_gb_collection_date}
\alias{parse_gb_collection_date}
\title{Parse a GenBank/BOLD-style collection-date string into a \code{Date}}
\usage{
parse_gb_collection_date(x)
}
\arguments{
\item{x}{Character vector of collection-date values}
}
\value{
\code{Date} vector, the same length as \code{x}
}
\description{
Collection dates in GenBank (and, less often, BOLD) records come in
several formats of varying precision -- \code{"DD-Mon-YYYY"} (the common
case), \code{"Mon-YYYY"}/\code{"YYYY-MM"}/\code{"YYYY"} (partial dates, normalized to
the first of the month/year), plus ISO \code{"YYYY-MM-DD"}. Occasionally the
field holds non-date junk instead (e.g. a locality string or a
coordinate pair that leaked into it upstream); that returns \code{NA}.
}
\details{
\code{as.Date()} is not used directly here: with an explicit \verb{format=} it
does \emph{partial}, not full-string, matching (\code{as.Date("58.298 N 8.539 E", format = "\%Y")} silently parses to the year 58 using today's month/day)
and formats lacking \verb{\%d} (\code{"\%b-\%Y"}, \code{"\%Y-\%m"}) fail outright rather
than defaulting the day. Each recognized pattern is instead converted to
an explicit ISO string first, so the final parse is unambiguous.
}
\examples{
parse_gb_collection_date(c("28-Sep-2014", "Sep-2023", "2020", "junk", NA))
}
