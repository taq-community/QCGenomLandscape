% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_table.R
\name{build_summary_dataframe}
\alias{build_summary_dataframe}
\title{Build the per-species genomic-data summary table}
\usage{
build_summary_dataframe(ncbi_results, genes_df, bdqc_taxo, ca_risk, qc_risk)
}
\arguments{
\item{ncbi_results}{Tibble as returned by \code{fetch_ncbi_sequences()$results}
(must have \code{organism}, \code{accession} columns)}

\item{genes_df}{Tibble as returned by \code{\link[=fetch_gene_annotations]{fetch_gene_annotations()}} (must
have \code{accession}, \code{gene} columns)}

\item{bdqc_taxo}{Tibble with columns \code{species}, \code{vernacular_fr},
\code{vernacular_en}, \code{group_en}}

\item{ca_risk, qc_risk}{Tibble with columns \code{species}, \code{status} (e.g. from
\code{\link[=load_risk_status]{load_risk_status()}})}
}
\value{
Tibble, one row per species
}
\description{
Pure, in-memory core of \code{create_dataframe.R}: joins NCBI sequence counts,
gene-marker counts, BDQC taxonomy, and conservation-risk status into one
row per species.
}
