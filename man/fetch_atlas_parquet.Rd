% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_comparison.R
\name{fetch_atlas_parquet}
\alias{fetch_atlas_parquet}
\title{Fetch a Biodiversité Québec Atlas public-data export, caching it locally}
\usage{
fetch_atlas_parquet(
  export_date = NULL,
  dest_dir = "data/atlas",
  base_url = "https://object-arbutus.cloud.computecanada.ca/bq-io/atlas/parquet/",
  list_dates_fn = function(url) {
     dates <- utils::read.csv(url, header = FALSE,
    col.names = "date")$date
     dates[nzchar(trimws(dates))]
 },
  download_fn = function(url, destfile) {
     utils::download.file(url, destfile, mode =
    "wb", quiet = FALSE)
 }
)
}
\arguments{
\item{export_date}{Character \code{"YYYY-MM-DD"}, default the latest export
listed in the bucket's \code{atlas_export_dates.csv}}

\item{dest_dir}{Directory to cache the downloaded parquet file in,
default \code{"data/atlas"}}

\item{base_url}{Base URL of the Atlas parquet exports}

\item{list_dates_fn}{Function with signature \code{(url)} returning the export
dates as a character vector, default reads \code{atlas_export_dates.csv};
injectable for testing without network access}

\item{download_fn}{Function with signature \verb{(url, destfile)}, default
\code{\link[utils:download.file]{utils::download.file()}}; injectable for testing without network access}
}
\value{
Character scalar, local path to the parquet file
}
\description{
Downloads via the same public, tokenless S3 bucket documented at
\code{source("http://atlas.biodiversite-quebec.ca/bq-atlas-parquet.R")} --
re-implemented directly (rather than \code{source()}-ing that URL) so the
fetch is inspectable and testable. Files are ~2 GB; already-downloaded
exports are reused, not re-fetched.
}
