% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_gene_prevalence}
\alias{plot_gene_prevalence}
\title{Donut chart: gene prevalence by taxonomic group}
\usage{
plot_gene_prevalence(
  genes_grouped,
  gene_groups = c("COI", "Photosynthesis-related (rbcL, matK, etc.)"),
  min_species = 5
)
}
\arguments{
\item{genes_grouped}{Tibble with columns \code{species}, \code{gene_group}, \code{groupe}
(gene classification joined to species and their finer taxonomic group,
e.g. via \code{\link[=assign_gene_group]{assign_gene_group()}} and \code{\link[=classify_taxon_group]{classify_taxon_group()}})}

\item{gene_groups}{Character vector of \code{gene_group} values to plot as
separate facets, default \code{c("COI", "Photosynthesis-related (rbcL, matK, etc.)")}}

\item{min_species}{Integer; gene groups with fewer than this many total
species (summed across taxonomic groups) are dropped, default 5}
}
\value{
A \code{ggplot} object (caller is responsible for \code{ggplot2::ggsave()})
}
\description{
Donut chart: gene prevalence by taxonomic group
}
