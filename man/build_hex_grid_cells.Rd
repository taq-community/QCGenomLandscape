% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_comparison.R
\name{build_hex_grid_cells}
\alias{build_hex_grid_cells}
\title{Build a full hexagonal grid over a boundary, with stable cell IDs}
\usage{
build_hex_grid_cells(boundary_sf, cellsize = 10000, crs_proj = 3347)
}
\arguments{
\item{boundary_sf}{\code{sf} polygon object defining the grid extent}

\item{cellsize}{Numeric, grid cell size in meters (in \code{crs_proj}), default 10000}

\item{crs_proj}{Numeric EPSG code for the projection used to build the
grid, default \code{3347} (Canada Lambert Conformal Conic)}
}
\value{
\code{sf} polygon grid (WGS84) with a \code{cell_id} column
}
\description{
Unlike \code{\link[=make_hex_grid]{make_hex_grid()}} (which counts one point layer and drops empty
cells), this returns \emph{every} cell with a stable \code{cell_id} regardless of
occupancy, so multiple point layers (e.g. eDNA vs traditional
occurrences) can be joined against the same reusable grid and compared
cell-for-cell.
}
