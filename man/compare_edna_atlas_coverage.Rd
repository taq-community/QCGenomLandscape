% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_comparison.R
\name{compare_edna_atlas_coverage}
\alias{compare_edna_atlas_coverage}
\title{Compare eDNA vs traditional occurrence coverage against the Atlas, on a hex grid}
\usage{
compare_edna_atlas_coverage(
  atlas_parquet_path,
  hex_grid,
  edna_occurrences,
  bdqc_species = NULL,
  molecular_datasets = atlas_molecular_datasets(),
  con = NULL
)
}
\arguments{
\item{atlas_parquet_path}{Character, local path to an Atlas parquet
export, e.g. from \code{\link[=fetch_atlas_parquet]{fetch_atlas_parquet()}}}

\item{hex_grid}{\code{sf} polygon grid with a \code{cell_id} column, e.g. from
\code{\link[=build_hex_grid_cells]{build_hex_grid_cells()}}}

\item{edna_occurrences}{Tibble with columns \code{species}, \code{lon}, \code{lat},
\code{date}, e.g. from \code{\link[=extract_edna_occurrences]{extract_edna_occurrences()}}}

\item{bdqc_species}{Character vector of species to restrict the
comparison to, or \code{NULL} for no restriction (default \code{NULL})}

\item{molecular_datasets}{Character vector of Atlas \code{dataset_name} values
to treat as eDNA, default \code{\link[=atlas_molecular_datasets]{atlas_molecular_datasets()}}}

\item{con}{An existing DBI connection to a DuckDB database, or \code{NULL}
(default) to open and close a temporary one; injectable for testing}
}
\value{
A list with \code{edna_agg} and \code{traditional_agg} tibbles, each with
columns \code{species}, \code{cell_id}, \code{year_obs}, \code{n}
}
\description{
The heavy lifting: classifies each Atlas record as \code{"eDNA"} (one of
\code{\link[=atlas_molecular_datasets]{atlas_molecular_datasets()}}) or \code{"traditional"}, spatially bins every
record into \code{hex_grid} via a DuckDB spatial join (fast even at Atlas
scale -- tens of millions of rows join against a ~18k-cell grid in well
under a minute), and folds in \code{edna_occurrences} (e.g. from
\code{\link[=extract_edna_occurrences]{extract_edna_occurrences()}}) as additional eDNA evidence, binned into
the same grid via \code{\link[sf:st_join]{sf::st_join()}}.
}
\details{
No deduplication is attempted between \code{edna_occurrences} and Atlas's own
molecular datasets (some of which, e.g. iBOL, share underlying records
with a BOLD-sourced \code{edna_occurrences}) -- this has little effect on the
presence-based metrics \code{\link[=summarize_edna_contribution]{summarize_edna_contribution()}} computes from the
result, which count distinct (species, cell) / (species, year) pairs, not
raw record volume.
}
